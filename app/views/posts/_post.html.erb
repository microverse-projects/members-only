<div class="posts">
  <div>
    <h3 class="post"><%= post.title %></h3>
    <% if logged_in? %>
    	<h4 class="author">
    		<%= User.find_by(id: post.user_id).name  %>
    	</h4>
    <% end %>
    <p><%= post.content %></p>
  </div>
</div>
<hr>