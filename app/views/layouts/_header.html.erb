<header>

  <nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#member-nav" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "members-only", root_path,class: 'navbar-brand' %>
        </div>

      <div class="collapse navbar-collapse" id="member-nav">
        <ul class="nav navbar-nav navbar-right">
          
          <% unless logged_in? %>
            <li>
            <%= link_to "Sign up", signup_path %>
            </li>
            <li>
              <%= link_to "Login", login_path %>
            </li>
          <% end %>
          <li>
            <%= link_to "All Posts", posts_path %>
          </li>
          <% if logged_in? %> 
            <li>
              <%= link_to "New Post", posts_new_path %> 
            </li>
             <li>
              <%= link_to "Logout", logout_path, method: :delete, class: "logout" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>